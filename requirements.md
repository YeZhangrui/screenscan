# 屏幕扫描助手 — 需求文档（确认版 v1.0）

> 状态：已与需求方确认（2025 年本地确认）
> 平台：Windows 11（64 位）
> 交付形式：单个 exe 免安装绿色版（双击即用，不依赖任何运行环境）

---

## 1. 产品定位

一款运行在 Windows 11 上的**屏幕识别辅助工具**。软件常驻后台，通过全局快捷键快速呼出，自动识别屏幕或图片上的**文字**与**图片**，用户可以复制文字、复制/保存图片。识别全部在**本地离线**完成，截图内容不上传、不联网。

## 2. 功能需求

### 2.1 核心功能

| 编号 | 功能 | 详细说明 |
|------|------|----------|
| F1 | 全局快捷键呼出 | 默认 **Alt+S**：直接进入框选截图识别；**Alt+Shift+S**：全屏扫描。快捷键在「设置」中可修改 |
| F2 | 框选截图识别 | 按热键 → 软件界面隐藏 → 屏幕出现暗色罩层与十字线 → 鼠标框选区域 → 松开后自动识别该区域 |
| F3 | 全屏扫描 | 一键识别整个屏幕；结果中标注每段文字所在位置 |
| F4 | 上传图片识别 | 选择本地图片（jpg / png / bmp / webp 等），识别其中的文字 |
| F5 | 文字复制 | 识别结果按行展示；支持单行复制、全部复制、一键导出 .txt |
| F6 | 图片复制 | ① 截图区域可「复制为图片 / 保存为 png」；② 结果窗内可再次框选某子区域单独抠出，复制/保存（用于提取图中图片素材） |
| F7 | 识别历史 | 保留最近 50 条（文字 + 截图缩略图），可回看、再次复制、导出、删除 |
| F8 | 结果窗置顶 | 识别结果小窗可钉在屏幕最上层；设置中可自由开关 |
| F9 | 开机自启 | 设置中可开关；开启后开机自动在后台待命 |
| F10 | 系统托盘 | 常驻托盘图标：单击显示主窗口；右键菜单（显示主窗口 / 全屏识别 / 退出） |

### 2.2 界面要求

- 简洁、直观；全中文界面；
- 主色调：淡蓝色（浅蓝背景 + 白色卡片 + 深蓝强调按钮），圆角卡片布局；
- 主窗口：顶部三个大按钮【框选识别】【全屏识别】【上传图片】；下方「识别结果」「历史记录」页签；右上角「设置」入口；
- 结果弹窗：截图缩略图 + 文字结果列表 + 操作按钮（复制文字 / 复制全部 / 复制图片 / 提取图中图片 / 导出txt / 另存图片 / 关闭）；
- 快捷键与设置项均有友好说明文字。

## 3. 数据与隐私

- 全部数据存本机，**不上传任何内容**，断网可用；
- 设置文件：`%LOCALAPPDATA%\ScreenScan\config.json`；
- 历史记录：`%LOCALAPPDATA%\ScreenScan\history\`（索引 + 截图 png）。

## 4. 技术方案（已定）

| 项 | 选择 | 理由 |
|----|------|------|
| 语言/环境 | Python 3.11 | 生态成熟，打包后用户无需安装 Python |
| 界面框架 | PySide6 (Qt) | 原生观感、托盘支持好、多分辨率适配稳 |
| OCR 引擎 | RapidOCR（onnxruntime 运行时） | 本地离线、免费、断网可用、中英文准确率高、轻量易打包 |
| 截图 | QScreen.grabWindow | 原生适配多显示器与 100%–150% 缩放 |
| 全局热键 | pynput | Windows 下无需管理员权限，支持动态改键 |
| 打包 | PyInstaller 单文件 exe | 双击即用绿色版 |

## 5. 已知限制与边界（必须如实告知用户）

1. exe 体积预计 120–200MB（含识别引擎）；首次启动/首次识别可能需 1–2 秒加载；
2. 识别精度：清晰印刷体、屏幕文字（中英文）效果好；潦草手写、艺术字、极小字可能不准；
3. 系统限制：UAC 管理员弹窗、部分独占全屏游戏画面无法截取（Windows 限制）；
4. PyInstaller 单文件可能被杀毒软件误报；若遇拦截提供文件夹版打包替代；
5. 「提取图中图片素材」v1 采用结果窗内再框选抠图；自动检测图片矩形框为后续增强项（可选）。

## 6. 验收清单（交付时逐条打勾）

- [ ] 双击 exe 即用，无需安装任何环境；托盘图标正常
- [ ] 默认 Alt+S：界面隐藏 → 十字线框选 → 自动识别 → 弹出结果窗
- [ ] 全屏识别：整屏文字识别并标注位置
- [ ] 上传含中文图片：能识别文字并可复制
- [ ] 文字可单行复制、全部复制、导出 txt
- [ ] 区域截图可复制为图片、可保存；结果窗内可再框选抠出子图并复制
- [ ] 历史记录保存、回看、删除正常
- [ ] 设置：改热键立即生效；置顶开关生效；开机自启开关生效
- [ ] 无文字时友好提示；ESC / 右键取消截图
- [ ] 100% 与 150% 缩放、单/多显示器框选坐标准确
- [ ] 断网状态全部功能可用

## 7. 默认快捷键表

| 动作 | 默认热键 | 可否修改 |
|------|----------|----------|
| 框选截图识别 | Alt+S | 可 |
| 全屏扫描 | Alt+Shift+S | 可 |
| 显示/隐藏主窗口 | 单击托盘图标 | — |

## 8. 版本规划

- v1.0：本文档全部内容
- v1.1+（可选增强）：图中图片矩形框自动检测、识别结果翻译、表格还原等
