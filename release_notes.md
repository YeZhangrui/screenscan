## 屏幕扫描助手 v1.5.1

Windows 11 **免安装单文件版**（双击即用，无需安装 Python 或任何运行环境）。

### 功能亮点

- **全局热键**：`Alt+S` 框选识别 / `Alt+Shift+S` 全屏扫描（可在设置中修改）
- **框选后截图立即留在屏幕前台**（显示「正在识别…」），识别完成**原地变为可交互面板**
- **屏幕上直接操作**：点击即可多选文字（无需 Ctrl）、拖拽框选、`Shift` 连选、点空白清空 → 一键复制
- **提取图片**：在截图上拖拽抠图，自动复制到剪贴板并可保存为 png
- **本地离线 OCR**（RapidOCR PP-OCRv6）：断网可用，截图内容不出本机
- 识别历史（50 条）、导出 txt、上传图片识别、结果窗置顶开关、开机自启
- **主题**：浅色 / 深色 / 跟随系统（系统切换深浅色实时跟随）
- 浮层面板可**拖拽缩放**（等比缩放，文字与识别框始终对齐）

### 系统要求

Windows 11（64 位）。首次启动约 1~3 秒加载识别引擎；本机实测框选区域约 1 秒、全屏约 2.6 秒出结果。

### 使用方法

1. 下载下方 `屏幕扫描助手.exe`
2. 双击运行（若被安全软件拦截，选择「信任 / 允许」）
3. 按 `Alt+S` 框选屏幕文字 → 结果直接浮在屏幕上 → 点击文字多选 → 「复制选中」

完整图文说明见仓库内 [docs/使用说明.md](https://github.com/YeZhangrui/screenscan/blob/main/docs/%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。
